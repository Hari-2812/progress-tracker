import { useState } from 'react';
import { Eye, EyeOff, LockKeyhole, Mail, UserRound } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import toast from 'react-hot-toast';
import AuthLayout from '../components/auth/AuthLayout';
import { useAuth } from '../context/AuthContext';

export default function Signup(){const [form,setForm]=useState({name:'',email:'',password:'',confirmPassword:''});const[show,setShow]=useState(false);const[busy,setBusy]=useState(false);const{signup}=useAuth();const navigate=useNavigate();const submit=async(e)=>{e.preventDefault();if(form.password.length<8)return toast.error('Password must be at least 8 characters');if(form.password!==form.confirmPassword)return toast.error('Passwords do not match');setBusy(true);try{await signup(form);toast.success('Your 90-day journey starts now!');navigate('/')}catch(err){toast.error(err.response?.data?.message||'Unable to create account')}finally{setBusy(false)}};return <AuthLayout title="Start your 90-day sprint" subtitle="Create your private space for consistent bank exam preparation."><form onSubmit={submit} className="space-y-4">{[[UserRound,'Full name','name','text','Your full name'],[Mail,'Email address','email','email','you@example.com']].map(([Icon,label,key,type,placeholder])=><label className="block" key={key}><span className="mb-2 block text-xs font-bold">{label}</span><div className="relative"><Icon className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={18}/><input className="field pl-11" type={type} required placeholder={placeholder} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/></div></label>)}{[['Password','password'],['Confirm password','confirmPassword']].map(([label,key])=><label className="block" key={key}><span className="mb-2 block text-xs font-bold">{label}</span><div className="relative"><LockKeyhole className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" size={18}/><input className="field px-11" type={show?'text':'password'} required placeholder={key==='password'?'Minimum 8 characters':'Repeat your password'} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/>{key==='password'&&<button type="button" onClick={()=>setShow(!show)} className="absolute right-4 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button>}</div></label>)}<button disabled={busy} className="primary-btn mt-2 w-full">{busy?'Creating account…':'Begin my 90-day journey'}</button><p className="text-center text-sm text-slate-500">Already have an account? <Link className="font-bold text-primary-600" to="/login">Sign in</Link></p></form></AuthLayout>}
